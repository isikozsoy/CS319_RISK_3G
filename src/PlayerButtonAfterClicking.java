import javafx.scene.control.Button;

public class PlayerButtonAfterClicking extends PlayerButton {
    //get player as a parameter
    PlayerButtonAfterClicking( double locX, double locY) {
        super( locX, locY);
    }
}